name: Health Check

on:
  schedule:
    - cron: '*/20 * * * *'   # Setiap 20 menit
  workflow_dispatch:           # Bisa run manual

jobs:
  health-check:
    runs-on: ubuntu-latest
    steps:
      - name: Check health endpoint
        run: |
          echo "✅ Health check triggered at $(date)"
          # Ganti dengan endpoint VPS kamu nanti
          # curl -f https://your-vps.com:8080/health || echo "⚠️ Bot down!"
